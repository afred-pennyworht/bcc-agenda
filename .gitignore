assets/speakers/*.mp4
assets/speakers/gif/*.gif
scripts/publish.log
test-results/
.claude/secrets.json

# Defensive: keep non-public artifacts out of this PUBLIC repo (temporary safety net;
# non-public material is being moved to a separate private repo).
accounting/
*.zip
.claude/settings.local.json
.claude/scheduled_tasks.lock
excalidraw.log

# Operational assets (sources viven en GDrive; pesados, no van al widget repo).
assets/brand/
assets/fonts/
assets/pictures/
assets/speakers/pics/
assets/sponsors/
assets/video/

# Sting pipeline (vive en repo dedicado bcc2026-stings).
remotion/

# Operational inbox (design handoffs, photo drops — privado).
inbox/

# Drafts pre-evento (históricos, archivados localmente).
BCC2026_Sponsors_Anunciados.txt
nostr_drafts_sponsors.md
